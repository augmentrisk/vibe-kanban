# Vibe Kanban Environment Variables
# Copy this file to .env and fill in the values

# =============================================================================
# GitHub OAuth (for local multi-user authentication)
# =============================================================================
# To enable GitHub SSO authentication:
# 1. Go to https://github.com/settings/developers
# 2. Create a new OAuth App with:
#    - Homepage URL: Your app URL (e.g., http://localhost:3000 or https://your-domain.com)
#    - Authorization callback URL: {APP_URL}/api/local-auth/github/callback
# 3. Copy the Client ID and generate a Client Secret

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# =============================================================================
# Application URL (for production deployments)
# =============================================================================
# The public URL where your app is accessible. Used for OAuth callbacks.
# For local development, defaults to http://localhost:3000
# For production, set this to your public domain (e.g., https://your-domain.com)

APP_URL=

# Or you can set the full redirect URI directly:
# GITHUB_REDIRECT_URI=https://your-domain.com/api/local-auth/github/callback

# =============================================================================
# Session Security
# =============================================================================
# Random string (32+ characters) for signing JWT tokens
# Generate with: openssl rand -hex 32

SESSION_SECRET=

# =============================================================================
# Optional: Development Settings
# =============================================================================
# Uncomment and set these to override default ports

# FRONTEND_PORT=3001
# BACKEND_PORT=3000
# HOST=localhost
