# Vibe Kanban Environment Variables
# Copy this file to .env and fill in the values

# =============================================================================
# GitHub OAuth (for local multi-user authentication)
# =============================================================================
# To enable GitHub SSO authentication:
# 1. Go to https://github.com/settings/developers
# 2. Create a new OAuth App with:
#    - Homepage URL: Your app URL (e.g., http://localhost:3000 or https://your-domain.com)
#    - Authorization callback URL: {APP_HOST}:{APP_PORT}/api/local-auth/github/callback
# 3. Copy the Client ID and generate a Client Secret

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# =============================================================================
# Application URL (required for GitHub OAuth)
# =============================================================================
# APP_HOST: The scheme and hostname (e.g., http://localhost or https://your-domain.com)
# APP_PORT: The port number (e.g., 3000 or 443)
# These are combined to build the OAuth callback URL: {APP_HOST}:{APP_PORT}/api/local-auth/github/callback

APP_HOST=
APP_PORT=

# =============================================================================
# Session Security
# =============================================================================
# Random string (32+ characters) for signing JWT tokens
# Generate with: openssl rand -hex 32

SESSION_SECRET=

# =============================================================================
# Optional: Development Settings
# =============================================================================
# Uncomment and set these to override default ports

# FRONTEND_PORT=3001
# BACKEND_PORT=3000
# HOST=localhost
